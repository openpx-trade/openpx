---
title: "fetch_user_trades"
sidebarTitle: "Fetch user trades"
openapi: GET /v1/trades/fetch_user_trades
playground: simple
description: "Fetch a user's trade history. Distinct from `fetch_fills` because some"
---

venues (Polymarket) carry on-chain fields like `tx_hash` and a
`realized_pnl` that don't fit the `Fill` model. When
`req.user_address` is `None`, returns the authenticated caller's own
trades. When `Some(addr)`, returns a public lookup for that wallet
(Polymarket only — Kalshi returns `NotSupported` in that case).

## Parameters

<ParamField path="cursor" type="string?" />

<ParamField path="end_ts" type="i64?">
  Unix seconds (inclusive)
</ParamField>

<ParamField path="limit" type="integer?" />

<ParamField path="market_id" type="string?" />

<ParamField path="side" type="OrderSide?" />

<ParamField path="start_ts" type="i64?">
  Unix seconds (inclusive)
</ParamField>

<ParamField path="user_address" type="string?">
  `None` = caller's own trades (auth required on both venues). `Some(addr)` = public lookup for `addr` (Polymarket only; Kalshi returns `NotSupported`).
</ParamField>

## Returns

<ResponseField name="trades" type="UserTrade[]">
  One page of `UserTrade` results.

  <Expandable title="UserTrade">
    <ResponseField name="asset_id" type="string?" />
    <ResponseField name="condition_id" type="string?" />
    <ResponseField name="fee" type="f64?" />
    <ResponseField name="id" type="string" />
    <ResponseField name="market_id" type="string" />
    <ResponseField name="owner" type="string?" />
    <ResponseField name="price" type="f64" />
    <ResponseField name="realized_pnl" type="f64?" />
    <ResponseField name="role" type="LiquidityRole?" />
    <ResponseField name="side" type="OrderSide" />
    <ResponseField name="size" type="f64" />
    <ResponseField name="ts_ms" type="i64" />
    <ResponseField name="tx_hash" type="string?" />
  </Expandable>
</ResponseField>
<ResponseField name="cursor" type="string?">Opaque pagination cursor. `null` on the last page.</ResponseField>

<RequestExample>

```rust Rust
let result = ex.fetch_user_trades(req).await?;
```

```python Python
result = ex.fetch_user_trades(req)
```

```typescript TypeScript
const result = await ex.fetchUserTrades(req);
```

</RequestExample>
<ResponseExample>

```json 200
{
  "trades": [
    {
      "asset_id": "example",
      "condition_id": "example",
      "fee": 0.5,
      "id": "example",
      "market_id": "example",
      "owner": "example",
      "price": 0.5,
      "realized_pnl": 0.5,
      "role": {},
      "side": {},
      "size": 0.5,
      "ts_ms": 0,
      "tx_hash": "example"
    }
  ],
  "cursor": null
}
```

</ResponseExample>
