---
title: "fetch_series"
sidebarTitle: "Fetch series"
openapi: GET /v1/series/fetch_series
playground: simple
description: "Fetch a page of series. A series is a recurring family of events"
---

(weekly inflation prints, monthly NFP, sports seasons, etc.).
Returns `(series, next_cursor)`.

## Parameters

<ParamField path="category" type="string?">
  Filter by category (e.g. `Politics`, `Sports`).
</ParamField>

<ParamField path="cursor" type="string?" />

<ParamField path="include_volume" type="boolean?">
  Include traded volume in the response when the venue supports it.
</ParamField>

<ParamField path="limit" type="integer?" />

## Returns

<ResponseField name="series" type="Series[]">
  One page of `Series` results.

  <Expandable title="Series">
    <ResponseField name="category" type="string?" />
    <ResponseField name="fee_type" type="string?" />
    <ResponseField name="frequency" type="string?" />
    <ResponseField name="id" type="string" />
    <ResponseField name="last_updated_ts" type="DateTime?" />
    <ResponseField name="settlement_sources" type="SettlementSource[]?" />
    <ResponseField name="tags" type="string[]?" />
    <ResponseField name="title" type="string" />
    <ResponseField name="volume" type="f64?" />
  </Expandable>
</ResponseField>
<ResponseField name="cursor" type="string?">Opaque pagination cursor. `null` on the last page.</ResponseField>

<RequestExample>

```rust Rust
let result = ex.fetch_series(req).await?;
```

```python Python
result = ex.fetch_series(req)
```

```typescript TypeScript
const result = await ex.fetchSeries(req);
```

</RequestExample>
<ResponseExample>

```json 200
{
  "series": [
    {
      "category": "example",
      "fee_type": "example",
      "frequency": "example",
      "id": "example",
      "last_updated_ts": "2026-01-01T00:00:00Z",
      "settlement_sources": [
        null
      ],
      "tags": [
        null
      ],
      "title": "example",
      "volume": 0.5
    }
  ],
  "cursor": null
}
```

</ResponseExample>
