---
title: "fetch_series_one"
sidebarTitle: "Fetch series one"
openapi: GET /v1/series/fetch_series_one
playground: simple
description: "Fetch a single series by ID or ticker."
---

## Parameters

<ParamField path="id" type="string" required />

## Returns

<ResponseField name="result" type="Series">
  `Series` payload.

  <Expandable title="Series">
    <ResponseField name="category" type="string?" />
    <ResponseField name="fee_type" type="string?" />
    <ResponseField name="frequency" type="string?" />
    <ResponseField name="id" type="string" />
    <ResponseField name="last_updated_ts" type="DateTime?" />
    <ResponseField name="settlement_sources" type="SettlementSource[]?" />
    <ResponseField name="tags" type="string[]?" />
    <ResponseField name="title" type="string" />
    <ResponseField name="volume" type="f64?" />
  </Expandable>
</ResponseField>

<RequestExample>

```rust Rust
let result = ex.fetch_series_one("id-example").await?;
```

```python Python
result = ex.fetch_series_one(id)
```

```typescript TypeScript
const result = await ex.fetchSeriesOne(id);
```

</RequestExample>
<ResponseExample>

```json 200
{
  "category": "example",
  "fee_type": "example",
  "frequency": "example",
  "id": "example",
  "last_updated_ts": "2026-01-01T00:00:00Z",
  "settlement_sources": [
    {}
  ],
  "tags": [
    "example"
  ],
  "title": "example",
  "volume": 0.5
}
```

</ResponseExample>
