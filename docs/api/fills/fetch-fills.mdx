---
title: "fetch_fills"
sidebarTitle: "Fetch fills"
openapi: GET /v1/fills/fetch_fills
playground: simple
description: "Fetch user's fill (trade execution) history for a market."
---

## Parameters

<ParamField path="market_id" type="string?" />

<ParamField path="limit" type="usize?" />

## Returns

<ResponseField name="result" type="Fill[]">
  List of `Fill` results.

  <Expandable title="Fill">
    <ResponseField name="created_at" type="DateTime" />
    <ResponseField name="fee" type="f64" />
    <ResponseField name="fill_id" type="string" />
    <ResponseField name="is_taker" type="boolean" />
    <ResponseField name="market_id" type="string" />
    <ResponseField name="order_id" type="string" />
    <ResponseField name="outcome" type="string" />
    <ResponseField name="price" type="f64" />
    <ResponseField name="side" type="OrderSide" />
    <ResponseField name="size" type="f64" />
  </Expandable>
</ResponseField>

<RequestExample>

```rust Rust
let result = ex.fetch_fills("market_id-example", 0).await?;
```

```python Python
result = ex.fetch_fills(market_id, limit)
```

```typescript TypeScript
const result = await ex.fetchFills(market_id, limit);
```

</RequestExample>
<ResponseExample>

```json 200
[
  {
    "created_at": "2026-01-01T00:00:00Z",
    "fee": 0.5,
    "fill_id": "example",
    "is_taker": true,
    "market_id": "example",
    "order_id": "example",
    "outcome": "example",
    "price": 0.5,
    "side": {},
    "size": 0.5
  }
]
```

</ResponseExample>
