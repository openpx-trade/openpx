---
title: "fetch_orderbook"
sidebarTitle: "Fetch orderbook"
openapi: GET /v1/orderbook/fetch_orderbook
playground: simple
description: "Fetch L2 orderbook for a market outcome."
---

Uses owned types for async compatibility.

## Parameters

<ParamField path="market_id" type="string" required />

<ParamField path="outcome" type="string?" />

<ParamField path="token_id" type="string?" />

## Returns

<ResponseField name="result" type="Orderbook">
  `Orderbook` payload.

  <Expandable title="Orderbook">
    <ResponseField name="asks" type="PriceLevel[]" />
    <ResponseField name="asset_id" type="string" />
    <ResponseField name="bids" type="PriceLevel[]" />
    <ResponseField name="hash" type="string?">Exchange-provided hash for verifying book state integrity during replay. Polymarket: present on `book` snapshot events.</ResponseField>
    <ResponseField name="last_update_id" type="integer?" />
    <ResponseField name="market_id" type="string" />
    <ResponseField name="timestamp" type="DateTime?" />
  </Expandable>
</ResponseField>

<RequestExample>

```rust Rust
let result = ex.fetch_orderbook(req).await?;
```

```python Python
result = ex.fetch_orderbook(req)
```

```typescript TypeScript
const result = await ex.fetchOrderbook(req);
```

</RequestExample>
<ResponseExample>

```json 200
{
  "asks": [
    {}
  ],
  "asset_id": "example",
  "bids": [
    {}
  ],
  "hash": "example",
  "last_update_id": 0,
  "market_id": "example",
  "timestamp": "2026-01-01T00:00:00Z"
}
```

</ResponseExample>
