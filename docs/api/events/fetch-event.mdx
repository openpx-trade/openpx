---
title: "fetch_event"
sidebarTitle: "Fetch event"
openapi: GET /v1/events/fetch_event
playground: simple
description: "Fetch a single event by ID or slug (whichever the venue accepts)."
---

## Parameters

<ParamField path="id" type="string" required />

## Returns

<ResponseField name="result" type="Event">
  `Event` payload.

  <Expandable title="Event">
    <ResponseField name="category" type="string?" />
    <ResponseField name="description" type="string?" />
    <ResponseField name="end_ts" type="DateTime?" />
    <ResponseField name="id" type="string" />
    <ResponseField name="last_updated_ts" type="DateTime?" />
    <ResponseField name="market_ids" type="string[]?" />
    <ResponseField name="mutually_exclusive" type="boolean?" />
    <ResponseField name="open_interest" type="f64?" />
    <ResponseField name="series_id" type="string?" />
    <ResponseField name="slug" type="string?" />
    <ResponseField name="start_ts" type="DateTime?" />
    <ResponseField name="status" type="string?" />
    <ResponseField name="title" type="string" />
    <ResponseField name="volume" type="f64?" />
  </Expandable>
</ResponseField>

<RequestExample>

```rust Rust
let result = ex.fetch_event("id-example").await?;
```

```python Python
result = ex.fetch_event(id)
```

```typescript TypeScript
const result = await ex.fetchEvent(id);
```

</RequestExample>
<ResponseExample>

```json 200
{
  "category": "example",
  "description": "example",
  "end_ts": "2026-01-01T00:00:00Z",
  "id": "example",
  "last_updated_ts": "2026-01-01T00:00:00Z",
  "market_ids": [
    "example"
  ],
  "mutually_exclusive": true,
  "open_interest": 0.5,
  "series_id": "example",
  "slug": "example",
  "start_ts": "2026-01-01T00:00:00Z",
  "status": "example",
  "title": "example",
  "volume": 0.5
}
```

</ResponseExample>
