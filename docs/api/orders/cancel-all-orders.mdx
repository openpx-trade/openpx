---
title: "cancel_all_orders"
sidebarTitle: "Cancel all orders"
openapi: POST /v1/orders/cancel_all_orders
playground: simple
description: "Cancel all of the caller's open orders. When `market_id.is_some()`,"
---

cancel only orders on that market.

## Parameters

<ParamField path="market_id" type="string?" />

## Returns

<ResponseField name="result" type="Order[]">
  List of `Order` results.

  <Expandable title="Order">
    <ResponseField name="created_at" type="DateTime" />
    <ResponseField name="filled" type="f64" />
    <ResponseField name="id" type="string" />
    <ResponseField name="market_id" type="string" />
    <ResponseField name="outcome" type="string" />
    <ResponseField name="price" type="f64" />
    <ResponseField name="side" type="OrderSide" />
    <ResponseField name="size" type="f64" />
    <ResponseField name="status" type="OrderStatus" />
    <ResponseField name="updated_at" type="DateTime?" />
  </Expandable>
</ResponseField>

<RequestExample>

```rust Rust
let result = ex.cancel_all_orders("market_id-example").await?;
```

```python Python
result = ex.cancel_all_orders(market_id)
```

```typescript TypeScript
const result = await ex.cancelAllOrders(market_id);
```

</RequestExample>
<ResponseExample>

```json 200
[
  {
    "created_at": "2026-01-01T00:00:00Z",
    "filled": 0.5,
    "id": "example",
    "market_id": "example",
    "outcome": "example",
    "price": 0.5,
    "side": {},
    "size": 0.5,
    "status": {},
    "updated_at": "2026-01-01T00:00:00Z"
  }
]
```

</ResponseExample>
