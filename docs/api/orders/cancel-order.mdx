---
title: "cancel_order"
sidebarTitle: "Cancel order"
openapi: /openpx.openapi.yaml POST /v1/orders/cancel_order
---

Cancel an existing order by ID. The order_id is globally unique on
both exchanges, so no scope qualifier is required.
