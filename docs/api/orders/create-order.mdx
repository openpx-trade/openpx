---
title: "create_order"
sidebarTitle: "Create order"
openapi: POST /v1/orders/create_order
playground: simple
description: "Create order"
---

## Parameters

<ParamField path="market_id" type="string" required />

<ParamField path="outcome" type="string" required />

<ParamField path="side" type="OrderSide" required />

<ParamField path="price" type="f64" required />

<ParamField path="size" type="f64" required />

<ParamField path="params" type="{string: string}" required />

## Returns

<ResponseField name="result" type="Order">
  `Order` payload.

  <Expandable title="Order">
    <ResponseField name="created_at" type="DateTime" />
    <ResponseField name="filled" type="f64" />
    <ResponseField name="id" type="string" />
    <ResponseField name="market_id" type="string" />
    <ResponseField name="outcome" type="string" />
    <ResponseField name="price" type="f64" />
    <ResponseField name="side" type="OrderSide" />
    <ResponseField name="size" type="f64" />
    <ResponseField name="status" type="OrderStatus" />
    <ResponseField name="updated_at" type="DateTime?" />
  </Expandable>
</ResponseField>

<RequestExample>

```rust Rust
let result = ex.create_order("market_id-example", "outcome-example", side, 0.5, 0.5, params).await?;
```

```python Python
result = ex.create_order(market_id, outcome, side, price, size, params)
```

```typescript TypeScript
const result = await ex.createOrder(market_id, outcome, side, price, size, params);
```

</RequestExample>
<ResponseExample>

```json 200
{
  "created_at": "2026-01-01T00:00:00Z",
  "filled": 0.5,
  "id": "example",
  "market_id": "example",
  "outcome": "example",
  "price": 0.5,
  "side": "buy",
  "size": 0.5,
  "status": "pending",
  "updated_at": "2026-01-01T00:00:00Z"
}
```

</ResponseExample>
