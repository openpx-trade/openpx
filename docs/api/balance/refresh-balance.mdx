---
title: "refresh_balance"
sidebarTitle: "Refresh balance"
openapi: POST /v1/balance/refresh_balance
playground: simple
description: "Refresh cached balance/allowance state if supported by the exchange."
---

<RequestExample>

```rust Rust
let result = ex.refresh_balance().await?;
```

```python Python
result = ex.refresh_balance()
```

```typescript TypeScript
const result = await ex.refreshBalance();
```

</RequestExample>
