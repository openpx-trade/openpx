---
title: "Positions"
description: "Read open positions."
---

## `fetch_positions`

```rust
async fn fetch_positions(
    market_id: Option<&str>,
) -> Result<Vec<Position>, OpenPxError>
```

Returns all open positions, optionally filtered to one market. See
[`Position`](/api/unified-schema#position) for fields.

The Rust `Position` exposes convenience methods: `cost_basis()`,
`current_value()`, `unrealized_pnl()`, `unrealized_pnl_percent()`.

<CodeGroup>

```rust Rust
let positions = ex.fetch_positions(None).await?;
for p in &positions {
    println!("{} {}: {} @ {}", p.market_id, p.outcome, p.size, p.average_price);
}
```

```python Python
for p in ex.fetch_positions():
    print(p.market_id, p.outcome, p.size, p.average_price)
```

```typescript TypeScript
const positions = await ex.fetchPositions();
```

</CodeGroup>
