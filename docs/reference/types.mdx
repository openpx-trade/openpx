---
title: "Types"
description: "Auto-generated reference for every type exported by OpenPX. Source: schema/openpx.schema.json (built from Rust #[derive(schemars::JsonSchema)] annotations)."
---

<Note>
This page is auto-generated from `schema/openpx.schema.json` by `just docs`.
Edit the corresponding Rust types and doc-comments in `engine/core/src/`
and regenerate — do not hand-edit this page.
</Note>

## `ActivityFill`

| Field | Type | Description |
|---|---|---|
| `asset_id` | `string` |  |
| `exchange_ts_ms` | `['integer', 'null']` *(optional)* | Exchange-authoritative timestamp (millis since epoch). |
| `fee` | `['number', 'null']` *(optional)* | Fee charged for this fill, when the exchange publishes one. |
| `fill_id` | `['string', 'null']` *(optional)* |  |
| `liquidity_role` | [`LiquidityRole`](#liquidityrole) \| `null` *(optional)* |  |
| `market_id` | `string` |  |
| `order_id` | `['string', 'null']` *(optional)* |  |
| `outcome` | `['string', 'null']` *(optional)* |  |
| `price` | `number` |  |
| `side` | `['string', 'null']` *(optional)* |  |
| `size` | `number` |  |
| `source_channel` | `string` |  |
| `tx_hash` | `['string', 'null']` *(optional)* | On-chain transaction hash, when the exchange publishes one. |


## `ActivityTrade`

Activity events still carried inside `WsUpdate::Trade` / `WsUpdate::Fill`. Retained as a typed payload alongside the stream rather than as a separate per-token surface.

`exchange_ts_ms` is exchange-authoritative millis since epoch — uniform with `WsUpdate::{Snapshot, Delta}::exchange_ts`, so every timestamp on the WS surface is the same type (`u64` millis). `chrono::DateTime` was more expressive but cost a representation mismatch at every FFI boundary.

| Field | Type | Description |
|---|---|---|
| `aggressor_side` | `['string', 'null']` *(optional)* |  |
| `asset_id` | `string` |  |
| `exchange_ts_ms` | `['integer', 'null']` *(optional)* | Exchange-authoritative timestamp (millis since epoch). |
| `fee_rate_bps` | `['integer', 'null']` *(optional)* | Fee rate in basis points (e.g. 0 = no fee, 200 = 2%). Polymarket `last_trade_price` events populate this. |
| `market_id` | `string` |  |
| `outcome` | `['string', 'null']` *(optional)* |  |
| `price` | `number` |  |
| `side` | `['string', 'null']` *(optional)* |  |
| `size` | `number` |  |
| `source_channel` | `string` |  |
| `trade_id` | `['string', 'null']` *(optional)* |  |


## `Candlestick`

OHLCV candlestick, normalized across all exchanges. Prices are decimals (0.0 to 1.0). Timestamp is the period START (not end). Serialized over the wire as RFC3339 (DateTime<Utc>) for API consistency.

| Field | Type | Description |
|---|---|---|
| `close` | `number` |  |
| `high` | `number` |  |
| `low` | `number` |  |
| `open` | `number` |  |
| `open_interest` | `['number', 'null']` *(optional)* | Open interest at this candle's close. Only available from exchanges that report it (e.g., Kalshi). |
| `timestamp` | `string` | Period start timestamp (UTC). lightweight-charts expects start-of-period. |
| `volume` | `number` | Trade volume in contracts. 0.0 if exchange doesn't provide volume. |


## `Event`

A grouping of related markets. On Kalshi, an Event is a single resolution (e.g. "Will Candidate X win State Y?") with one or more contracts. On Polymarket, an Event is the parent of one or more Markets sharing a common theme (e.g. "2028 US Presidential Election" with markets per candidate).

| Field | Type | Description |
|---|---|---|
| `category` | `['string', 'null']` *(optional)* |  |
| `description` | `['string', 'null']` *(optional)* |  |
| `end_ts` | `['string', 'null']` *(optional)* |  |
| `id` | `string` |  |
| `last_updated_ts` | `['string', 'null']` *(optional)* |  |
| `market_ids` | `string`[] *(optional)* |  |
| `mutually_exclusive` | `['boolean', 'null']` *(optional)* |  |
| `open_interest` | `['number', 'null']` *(optional)* |  |
| `series_id` | `['string', 'null']` *(optional)* |  |
| `slug` | `['string', 'null']` *(optional)* |  |
| `start_ts` | `['string', 'null']` *(optional)* |  |
| `status` | `['string', 'null']` *(optional)* |  |
| `title` | `string` |  |
| `volume` | `['number', 'null']` *(optional)* |  |


## `EventsRequest`

Request for fetching events.

| Field | Type | Description |
|---|---|---|
| `cursor` | `['string', 'null']` *(optional)* |  |
| `limit` | `['integer', 'null']` *(optional)* |  |
| `min_close_ts` | `['integer', 'null']` *(optional)* | Unix seconds — only events closing at or after this timestamp. |
| `min_updated_ts` | `['integer', 'null']` *(optional)* | Unix seconds — only events updated at or after this timestamp. |
| `series_id` | `['string', 'null']` *(optional)* | Filter by series ID / ticker. |
| `status` | `['string', 'null']` *(optional)* | Filter by lifecycle status — venue-specific values (e.g. `open`, `closed`, `settled` on Kalshi; `closed=true/false` on Polymarket). |
| `with_nested_markets` | `['boolean', 'null']` *(optional)* | Include the events' nested `Market` objects when supported. |


## `ExchangeInfo`

| Field | Type | Description |
|---|---|---|
| `has_approvals` | `boolean` |  |
| `has_cancel_all_orders` | `boolean` |  |
| `has_cancel_order` | `boolean` |  |
| `has_create_order` | `boolean` |  |
| `has_create_orders_batch` | `boolean` |  |
| `has_fetch_balance` | `boolean` |  |
| `has_fetch_event` | `boolean` |  |
| `has_fetch_events` | `boolean` |  |
| `has_fetch_fills` | `boolean` |  |
| `has_fetch_last_trade_price` | `boolean` |  |
| `has_fetch_market_tags` | `boolean` |  |
| `has_fetch_markets` | `boolean` |  |
| `has_fetch_midpoint` | `boolean` |  |
| `has_fetch_midpoints_batch` | `boolean` |  |
| `has_fetch_open_interest` | `boolean` |  |
| `has_fetch_orderbook` | `boolean` |  |
| `has_fetch_orderbook_history` | `boolean` |  |
| `has_fetch_orderbooks_batch` | `boolean` |  |
| `has_fetch_positions` | `boolean` |  |
| `has_fetch_price_history` | `boolean` |  |
| `has_fetch_series` | `boolean` |  |
| `has_fetch_series_one` | `boolean` |  |
| `has_fetch_server_time` | `boolean` |  |
| `has_fetch_spread` | `boolean` |  |
| `has_fetch_trades` | `boolean` |  |
| `has_fetch_user_activity` | `boolean` |  |
| `has_fetch_user_trades` | `boolean` |  |
| `has_refresh_balance` | `boolean` |  |
| `has_websocket` | `boolean` |  |
| `id` | `string` |  |
| `name` | `string` |  |


## `FetchMarketsParams`

| Field | Type | Description |
|---|---|---|
| `cursor` | `['string', 'null']` *(optional)* | Exchange-specific cursor (offset, page number, or cursor string) |
| `event_id` | `['string', 'null']` *(optional)* | Fetch all markets within a specific event. Pass a Kalshi event ticker (e.g., `"KXBTC-25MAR14"`) or a Polymarket event ID or slug (e.g., `"903"` or `"will-trump-win-2024"`) to get its child markets. When set, `series_id`, `cursor`, and `limit` are ignored (not paginated). `status` filtering is still applied client-side. |
| `limit` | `['integer', 'null']` *(optional)* | Per-page limit. Each exchange applies its own server-side cap: Kalshi tops out at 1000, Polymarket at ~500. Values above the cap are silently clamped to the cap. |
| `series_id` | `['string', 'null']` *(optional)* | Filter by series (Kalshi and Polymarket). Both exchanges organize markets as Series → Events → Markets. Pass a Kalshi series ticker (e.g., `"KXBTC"`) or a Polymarket series ID (e.g., `"10345"`) to fetch only markets in that series. |
| `status` | [`MarketStatusFilter`](#marketstatusfilter) \| `null` *(optional)* | Filter by market status. Defaults to Active at the exchange level when None. Use `MarketStatusFilter::All` to fetch markets of any status. |


## `FetchOrdersParams`

| Field | Type | Description |
|---|---|---|
| `market_id` | `['string', 'null']` *(optional)* |  |


## `FetchUserActivityParams`

| Field | Type | Description |
|---|---|---|
| `address` | `string` |  |
| `limit` | `['integer', 'null']` *(optional)* |  |


## `Fill`

A single fill (trade execution) from a user's order.

| Field | Type | Description |
|---|---|---|
| `created_at` | `string` |  |
| `fee` | `number` |  |
| `fill_id` | `string` |  |
| `is_taker` | `boolean` |  |
| `market_id` | `string` |  |
| `order_id` | `string` |  |
| `outcome` | `string` |  |
| `price` | `number` |  |
| `side` | [`OrderSide`](#orderside) |  |
| `size` | `number` |  |


## `InvalidationReason`

Why a specific book was invalidated — handed to users so they can decide whether to alert, log, or handle it silently.

**Variants:**

- `'Reconnect'`
- `'Lag'`
- `'ExchangeReset'`
- `SequenceGap`


## `LastTrade`

Last public trade price + side + size for a market outcome. Distinct from the full `MarketTrade` tape: this is just "what just printed?" — common UI need that doesn't require the full trade history.

| Field | Type | Description |
|---|---|---|
| `price` | `number` |  |
| `side` | [`OrderSide`](#orderside) |  |
| `size` | `number` |  |
| `ts_ms` | `integer` |  |


## `LiquidityRole`

**Variants:**

- `'maker'`
- `'taker'`


## `Market`

Unified prediction market model.

All exchanges produce this single type directly — no intermediate conversion.

# Price Format

All prices are normalized to decimal format (0.0 to 1.0). Exchange-specific conversions are handled during parsing:

- **Kalshi**: Fixed-point dollar strings parsed directly (post March 2026 migration). - **Polymarket**: Native prices already in decimal (0.0-1.0).

| Field | Type | Description |
|---|---|---|
| `accepting_orders` | `boolean` *(optional)* | Whether the market is currently accepting orders |
| `best_ask` | `['number', 'null']` *(optional)* | Best ask price (normalized 0-1) |
| `best_bid` | `['number', 'null']` *(optional)* | Best bid price (normalized 0-1) |
| `can_close_early` | `['boolean', 'null']` *(optional)* | Kalshi: can close early |
| `chain_id` | `['string', 'null']` *(optional)* | Chain ID for on-chain markets |
| `close_time` | `['string', 'null']` *(optional)* | Market close time |
| `condition_id` | `['string', 'null']` *(optional)* | Condition ID for CTF |
| `created_at` | `['string', 'null']` *(optional)* | Market creation time |
| `denomination_token` | `['string', 'null']` *(optional)* | Denomination token (e.g. USDC address) |
| `description` | `string` | Full description |
| `event_id` | `['string', 'null']` *(optional)* | Canonical OpenPX event ID for cross-exchange event grouping. |
| `exchange` | `string` | Exchange identifier (kalshi, polymarket) |
| `group_id` | `['string', 'null']` *(optional)* | Source-native event/group ID from the exchange. |
| `icon_url` | `['string', 'null']` *(optional)* | Market icon URL |
| `id` | `string` | Native exchange market ID |
| `image_url` | `['string', 'null']` *(optional)* | Market image URL |
| `last_trade_price` | `['number', 'null']` *(optional)* | Last trade price (normalized 0-1) |
| `liquidity` | `['number', 'null']` *(optional)* | Current liquidity |
| `maker_fee_bps` | `['number', 'null']` *(optional)* | Maker fee rate (basis points) |
| `market_type` | `any` | Market type classification |
| `min_order_size` | `['number', 'null']` *(optional)* | Minimum order size (contracts) |
| `native_numeric_id` | `['string', 'null']` *(optional)* | Polymarket's numeric DB id (e.g. "1031769"). Exposed for callers that need to build UI deep-links or cross-reference Polymarket's REST-only numeric surface. Not used for trading or subscription — `id` (the condition_id on Polymarket) is the canonical identifier. |
| `neg_risk` | `['boolean', 'null']` *(optional)* | Polymarket: neg-risk flag |
| `neg_risk_market_id` | `['string', 'null']` *(optional)* | Polymarket: neg-risk market ID |
| `notional_value` | `['number', 'null']` *(optional)* | Notional value per contract (Kalshi) |
| `open_interest` | `['number', 'null']` *(optional)* | Current open interest |
| `open_time` | `['string', 'null']` *(optional)* | Market open time |
| `openpx_id` | `string` | Primary key: {exchange}:{native_id} |
| `outcome_prices` | `object` *(optional)* | Outcome prices from the REST API (e.g., {"Yes": 0.65, "No": 0.35}) |
| `outcome_tokens` | [`OutcomeToken`](#outcometoken)[] *(optional)* | Outcome-to-token mapping for orderbook subscriptions |
| `outcomes` | `string`[] *(optional)* | Outcome labels (e.g., ["Yes", "No"] for binary markets) |
| `previous_price` | `['number', 'null']` *(optional)* | Kalshi: previous price |
| `price_change_1d` | `['number', 'null']` *(optional)* | 24-hour YES price change (decimal, e.g. 0.05 = +5%) |
| `price_change_1h` | `['number', 'null']` *(optional)* | 1-hour YES price change |
| `price_change_1mo` | `['number', 'null']` *(optional)* | 30-day YES price change |
| `price_change_1wk` | `['number', 'null']` *(optional)* | 7-day YES price change |
| `price_level_structure` | `['string', 'null']` *(optional)* | Kalshi sub-penny pricing structure |
| `question` | `['string', 'null']` *(optional)* | Market question (may differ from title) |
| `question_id` | `['string', 'null']` *(optional)* | Question ID (Polymarket) |
| `result` | `['string', 'null']` *(optional)* | Resolution result |
| `rules` | `['string', 'null']` *(optional)* | Resolution rules |
| `settlement_time` | `['string', 'null']` *(optional)* | Settlement / resolution time |
| `settlement_value` | `['number', 'null']` *(optional)* | Kalshi: settlement value |
| `slug` | `['string', 'null']` *(optional)* | URL-friendly identifier |
| `spread` | `['number', 'null']` *(optional)* | Bid-ask spread (decimal) |
| `status` | `any` | Normalized status: Active, Closed, Resolved |
| `taker_fee_bps` | `['number', 'null']` *(optional)* | Taker fee rate (basis points) |
| `tick_size` | `['number', 'null']` *(optional)* | Tick size (minimum price increment, normalized decimal e.g. 0.01) |
| `title` | `string` | Market title |
| `token_id_no` | `['string', 'null']` *(optional)* | No outcome token ID |
| `token_id_yes` | `['string', 'null']` *(optional)* | Yes outcome token ID |
| `volume` | `number` | Total volume (USD) |
| `volume_1mo` | `['number', 'null']` *(optional)* | 30-day rolling trading volume (USD) |
| `volume_1wk` | `['number', 'null']` *(optional)* | 7-day rolling trading volume (USD) |
| `volume_24h` | `['number', 'null']` *(optional)* | 24-hour trading volume (USD) |


## `MarketStatus`

Normalized market status across all exchanges.

**Variants:**

- `'active'`
- `'closed'`
- `'resolved'`


## `MarketStatusFilter`

Filter for market status in fetch queries.

Unlike `MarketStatus` (which represents a market's actual status), this enum includes an `All` variant for fetching markets regardless of status.

**Variants:**

- `'active'`
- `'closed'`
- `'resolved'`
- `'all'`


## `MarketTrade`

Normalized public market trade, suitable for "tape" UIs.

- `price` is normalized to [0.0, 1.0] across all exchanges. - `timestamp` is the exchange-provided trade timestamp (UTC).

| Field | Type | Description |
|---|---|---|
| `aggressor_side` | `['string', 'null']` *(optional)* |  |
| `id` | `['string', 'null']` *(optional)* |  |
| `no_price` | `['number', 'null']` *(optional)* |  |
| `outcome` | `['string', 'null']` *(optional)* |  |
| `price` | `number` |  |
| `side` | `['string', 'null']` *(optional)* |  |
| `size` | `number` |  |
| `source_channel` | `string` |  |
| `taker_address` | `['string', 'null']` *(optional)* |  |
| `timestamp` | `string` |  |
| `tx_hash` | `['string', 'null']` *(optional)* |  |
| `yes_price` | `['number', 'null']` *(optional)* |  |


## `MarketType`

Market type classification.

**Variants:**

- `'binary'`
- `'categorical'`
- `'scalar'`


## `MidpointRequest`

Request for midpoint / spread / last-trade-price methods. The same shape is reused for all three since they target the same outcome and accept the same identifier inputs.

| Field | Type | Description |
|---|---|---|
| `market_id` | `string` |  |
| `outcome` | `['string', 'null']` *(optional)* |  |
| `token_id` | `['string', 'null']` *(optional)* |  |


## `NewOrder`

One order in a `create_orders_batch` call. Each venue caps the batch size (Polymarket: 15; Kalshi: token-budget-dependent).

| Field | Type | Description |
|---|---|---|
| `client_order_id` | `['string', 'null']` *(optional)* | Kalshi-specific idempotency key. |
| `expiration_ts` | `['integer', 'null']` *(optional)* | Unix seconds. Required for `OrderType::Gtc` orders that should expire. |
| `market_id` | `string` |  |
| `order_type` | [`OrderType`](#ordertype) |  |
| `outcome` | `string` |  |
| `post_only` | `['boolean', 'null']` *(optional)* | Polymarket: pin maker-only. Ignored on Kalshi. |
| `price` | `number` |  |
| `reduce_only` | `['boolean', 'null']` *(optional)* | Kalshi: only allow size reductions. Maps to `reduce_only=true`. |
| `side` | [`OrderSide`](#orderside) |  |
| `size` | `number` |  |


## `Order`

| Field | Type | Description |
|---|---|---|
| `created_at` | `string` |  |
| `filled` | `number` |  |
| `id` | `string` |  |
| `market_id` | `string` |  |
| `outcome` | `string` |  |
| `price` | `number` |  |
| `side` | [`OrderSide`](#orderside) |  |
| `size` | `number` |  |
| `status` | [`OrderStatus`](#orderstatus) |  |
| `updated_at` | `['string', 'null']` *(optional)* |  |


## `OrderSide`

**Variants:**

- `'buy'`
- `'sell'`


## `OrderStatus`

**Variants:**

- `'pending'`
- `'open'`
- `'filled'`
- `'partially_filled'`
- `'cancelled'`
- `'rejected'`


## `OrderType`

Order time-in-force / execution type.

Normalized across all exchanges: - `Gtc` (good-til-cancelled) — rests on the book until filled or cancelled. - `Ioc` (immediate-or-cancel) — fills what it can immediately, cancels the rest. - `Fok` (fill-or-kill) — must fill entirely in one shot or is cancelled.

**Variants:**

- `'gtc'`
- `'ioc'`
- `'fok'`


## `Orderbook`

| Field | Type | Description |
|---|---|---|
| `asks` | [`PriceLevel`](#pricelevel)[] |  |
| `asset_id` | `string` |  |
| `bids` | [`PriceLevel`](#pricelevel)[] |  |
| `hash` | `['string', 'null']` *(optional)* | Exchange-provided hash for verifying book state integrity during replay. Polymarket: present on `book` snapshot events. |
| `last_update_id` | `['integer', 'null']` *(optional)* |  |
| `market_id` | `string` |  |
| `timestamp` | `['string', 'null']` *(optional)* |  |


## `OrderbookHistoryRequest`

| Field | Type | Description |
|---|---|---|
| `cursor` | `['string', 'null']` *(optional)* |  |
| `end_ts` | `['integer', 'null']` *(optional)* |  |
| `limit` | `['integer', 'null']` *(optional)* |  |
| `market_id` | `string` |  |
| `start_ts` | `['integer', 'null']` *(optional)* |  |
| `token_id` | `['string', 'null']` *(optional)* |  |


## `OrderbookRequest`

Request for fetching an L2 orderbook.

| Field | Type | Description |
|---|---|---|
| `market_id` | `string` |  |
| `outcome` | `['string', 'null']` *(optional)* |  |
| `token_id` | `['string', 'null']` *(optional)* |  |


## `OrderbookSnapshot`

A point-in-time L2 orderbook snapshot, used for historical orderbook data.

| Field | Type | Description |
|---|---|---|
| `asks` | [`PriceLevel`](#pricelevel)[] |  |
| `bids` | [`PriceLevel`](#pricelevel)[] |  |
| `hash` | `['string', 'null']` *(optional)* |  |
| `recorded_at` | `['string', 'null']` *(optional)* |  |
| `timestamp` | `string` |  |


## `OutcomeToken`

| Field | Type | Description |
|---|---|---|
| `outcome` | `string` |  |
| `token_id` | `string` |  |


## `Position`

| Field | Type | Description |
|---|---|---|
| `average_price` | `number` |  |
| `current_price` | `number` |  |
| `market_id` | `string` |  |
| `outcome` | `string` |  |
| `size` | `number` |  |


## `PriceHistoryInterval`

**Variants:**

- `'1m'`
- `'1h'`
- `'6h'`
- `'1d'`
- `'1w'`
- `'max'`


## `PriceHistoryRequest`

Request for fetching price history / candlestick data.

| Field | Type | Description |
|---|---|---|
| `condition_id` | `['string', 'null']` *(optional)* | Condition ID for OI enrichment (Polymarket). |
| `end_ts` | `['integer', 'null']` *(optional)* | Unix seconds |
| `interval` | [`PriceHistoryInterval`](#pricehistoryinterval) |  |
| `market_id` | `string` |  |
| `outcome` | `['string', 'null']` *(optional)* |  |
| `start_ts` | `['integer', 'null']` *(optional)* | Unix seconds |
| `token_id` | `['string', 'null']` *(optional)* |  |


## `PriceLevel`

| Field | Type | Description |
|---|---|---|
| `price` | [`number`](#number) |  |
| `size` | `number` |  |


## `PriceLevelChange`

A single price level change. Absolute replacement semantics: size > 0 = set level to this size, size == 0 = remove level.

| Field | Type | Description |
|---|---|---|
| `price` | [`number`](#number) |  |
| `side` | [`PriceLevelSide`](#pricelevelside) |  |
| `size` | `number` |  |


## `PriceLevelSide`

Bid or ask side. Serializes as "bid"/"ask" on the wire.

**Variants:**

- `'bid'`
- `'ask'`


## `Series`

A recurring family of events. Examples: a weekly inflation reading, a monthly nonfarm payrolls release, a regular sports season. On Kalshi, a Series is identified by `series_ticker` (e.g. `KXPRES`). On Polymarket, a Series wraps multiple Events with shared metadata.

| Field | Type | Description |
|---|---|---|
| `category` | `['string', 'null']` *(optional)* |  |
| `fee_type` | `['string', 'null']` *(optional)* |  |
| `frequency` | `['string', 'null']` *(optional)* |  |
| `id` | `string` |  |
| `last_updated_ts` | `['string', 'null']` *(optional)* |  |
| `settlement_sources` | [`SettlementSource`](#settlementsource)[] *(optional)* |  |
| `tags` | `string`[] *(optional)* |  |
| `title` | `string` |  |
| `volume` | `['number', 'null']` *(optional)* |  |


## `SeriesRequest`

Request for fetching series.

| Field | Type | Description |
|---|---|---|
| `category` | `['string', 'null']` *(optional)* | Filter by category (e.g. `Politics`, `Sports`). |
| `cursor` | `['string', 'null']` *(optional)* |  |
| `include_volume` | `['boolean', 'null']` *(optional)* | Include traded volume in the response when the venue supports it. |
| `limit` | `['integer', 'null']` *(optional)* |  |


## `SessionEvent`

Connection-level events, emitted on a channel separate from `WsUpdate` so a reconnect is observable as a single global signal rather than per-market.

**Variants:**

- `kind`
- `gap_ms`
- `kind`
- `dropped`
- `first_seq`
- `kind`
- `last_seq`
- `kind`
- `market_id`
- `reason`
- `kind`
- `message`


## `SettlementSource`

| Field | Type | Description |
|---|---|---|
| `name` | `['string', 'null']` *(optional)* |  |
| `url` | `['string', 'null']` *(optional)* |  |


## `Spread`

Top-of-book bid/ask + spread for a market outcome. Lighter than a full `Orderbook` — useful for liquidity scoring and PnL marks where only the best levels are needed.

| Field | Type | Description |
|---|---|---|
| `ask` | `number` |  |
| `bid` | `number` |  |
| `spread` | `number` |  |
| `ts_ms` | `['integer', 'null']` *(optional)* |  |


## `Tag`

A category or tag attached to a market or event. Used for filtering and search. Kalshi exposes tags via category-keyed lookups; Polymarket exposes per-market and per-event tag arrays.

| Field | Type | Description |
|---|---|---|
| `id` | `string` |  |
| `name` | `string` |  |
| `slug` | `['string', 'null']` *(optional)* |  |


## `TradesRequest`

Request for fetching recent public trades ("tape") for a market outcome.

| Field | Type | Description |
|---|---|---|
| `cursor` | `['string', 'null']` *(optional)* | Opaque pagination cursor from a previous response. |
| `end_ts` | `['integer', 'null']` *(optional)* | Unix seconds (inclusive) |
| `limit` | `['integer', 'null']` *(optional)* | Max number of trades to return (exchange-specific caps may apply). |
| `market_id` | `string` | Exchange-native market identifier. |
| `market_ref` | `['string', 'null']` *(optional)* | Optional alternate market identifier for trade endpoints (e.g., Polymarket conditionId). When provided, exchanges should prefer this over `market_id`. |
| `outcome` | `['string', 'null']` *(optional)* |  |
| `start_ts` | `['integer', 'null']` *(optional)* | Unix seconds (inclusive) |
| `token_id` | `['string', 'null']` *(optional)* |  |


## `UserTrade`

A user-facing trade row: distinct from `Fill` because it carries auxiliary fields some venues expose (realized PnL, on-chain tx hash, owner wallet) but others do not. Polymarket Data `/trades` returns these natively; Kalshi `/portfolio/fills` returns a subset, with `realized_pnl` and `tx_hash` left as `None`.

| Field | Type | Description |
|---|---|---|
| `asset_id` | `['string', 'null']` *(optional)* |  |
| `condition_id` | `['string', 'null']` *(optional)* |  |
| `fee` | `['number', 'null']` *(optional)* |  |
| `id` | `string` |  |
| `market_id` | `string` |  |
| `owner` | `['string', 'null']` *(optional)* |  |
| `price` | `number` |  |
| `realized_pnl` | `['number', 'null']` *(optional)* |  |
| `role` | [`LiquidityRole`](#liquidityrole) \| `null` *(optional)* |  |
| `side` | [`OrderSide`](#orderside) |  |
| `size` | `number` |  |
| `ts_ms` | `integer` |  |
| `tx_hash` | `['string', 'null']` *(optional)* |  |


## `UserTradesRequest`

Request for `fetch_user_trades`.

| Field | Type | Description |
|---|---|---|
| `cursor` | `['string', 'null']` *(optional)* |  |
| `end_ts` | `['integer', 'null']` *(optional)* | Unix seconds (inclusive) |
| `limit` | `['integer', 'null']` *(optional)* |  |
| `market_id` | `['string', 'null']` *(optional)* |  |
| `side` | [`OrderSide`](#orderside) \| `null` *(optional)* |  |
| `start_ts` | `['integer', 'null']` *(optional)* | Unix seconds (inclusive) |
| `user_address` | `['string', 'null']` *(optional)* | `None` = caller's own trades (auth required on both venues). `Some(addr)` = public lookup for `addr` (Polymarket only; Kalshi returns `NotSupported`). |


## `WsUpdate`

Every per-market event the WebSocket surface emits. Closed tagged union; no untyped escape hatch in the stable enum. If an exchange grows a payload we want to surface in raw form, add a separate `raw_events()` stream rather than another `WsUpdate` variant — keeps consumer `match` exhaustiveness honest.

**Variants:**

- `asset_id`
- `book`
- `exchange_ts`
- `kind`
- `local_ts_ms`
- `market_id`
- `seq`
- `asset_id`
- `changes`
- `exchange_ts`
- `kind`
- `local_ts_ms`
- `market_id`
- `seq`
- `asset_id`
- `kind`
- `local_ts_ms`
- `market_id`
- `reason`
- `seq`
- `kind`
- `local_ts_ms`
- `trade`
- `fill`
- `kind`
- `local_ts_ms`


## `number`

